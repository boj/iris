-- Atomic counter: write an initial value, add to it, read back.
-- Demonstrates atomic_write, atomic_add, and atomic_read.
let counter x : Int -> Int [cost: Const(3)] =
  let _ = atomic_write "count" x in
  let _ = atomic_add "count" 1 in
  atomic_read "count"
